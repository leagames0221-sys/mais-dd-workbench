# Week 0 — scaffold + audit phase (doctrine: external-source-audit 通過待ち、 全 ★★★ green flag 予定)
# Week 1+ で heavy deps (docling + llama-index + sentence-transformers + faiss) を追加 audit + install
# CVE-2025-71176 (pytest privilege escalation) fix 維持: pytest>=9.0.3

# Synthetic VDR data generation (T1 同 stack reuse)
faker>=25.0,<30.0
python-dotenv>=1.0,<2.0

# Test infra
pytest>=9.0.3,<10.0
pytest-cov>=7.1.0,<8.0

# Schema validation
pydantic>=2.5,<3.0

# Audit + drift CI
pip-audit>=2.7,<3.0
